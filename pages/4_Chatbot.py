import streamlit as st

st.write("Now you've done all of the setup steps necessary to create a custom chatbot!")
st.write("Start the server by doing 'uvicorn main:app --host 0.0.0.0 --port 8000' to run the chat server.")
st.write("You may have to do 'streamlit run _____.py; in order to get the chatbot UI running.")